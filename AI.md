# Project Context
This is the Forge project itself. When modifying Forge's own code:
- Keep all modules under 200 lines
- Use ESM imports throughout
- Test with `node bin/forge.js --help` after changes
